apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: mongo-pvc
  namespace: app-ns

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: manual

  resources:
    requests:
      storage: 512Mi
